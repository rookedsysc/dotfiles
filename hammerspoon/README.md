## HOW TO USE

```bash
cp -R ./* ~/.hammerspoon
```

reload hammerspoon config

NotificationCenter 알림 닫기 단축키(`ctrl` + `cmd` + `shift` + `]`)는 `scripts/close_notifications_applescript.js`를 실행하므로, Hammerspoon과 `osascript`/System Events에 손쉬운 사용 권한이 필요합니다.
